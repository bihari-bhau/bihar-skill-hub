[build]
builder = "nixpacks"

[deploy]
startCommand = "gunicorn wsgi:application --bind 0.0.0.0:$PORT"
restartPolicyType = "on_failure"
```

---

## Important — How to create this file correctly in VS Code
```
1. In VS Code → right click on backend/ folder
2. Click "New File"
3. Name it → railway.toml
4. Copy paste the content above
5. Make sure you use straight quotes " not curly quotes "
6. Save the file (Ctrl + S)